<h1> Create New Figure </h1>

<form action="/figures" method="post">

  <label>Name</label>
  <input type="text" name="figure[name]" id="figure_name">

  <h2><label>Titles</label></h2>
  <% Title.all.each do |title| %>
  <input type="checkbox" name="figure[title_ids][]" id="title_<%=title.id%>"> value="<%=title.id%>"<label><%= title.name %></label>
  <% end %>

  <h2><label>Create New Title</label></h2>
  <input type="text" name="title[name]" id="new_title">

  <h2><label>Landmarks</label></h2>
  <% Landmark.all.each do |landmark| %>
  <input type="checkbox" name="figure[landmark]" id="figure_landmark"><label><%= landmark.name %></label>
  <% end %>

  <h2><lable>Create New Landmark</lable></h2>
  <input type="text" name="figure[landmark]" id="figure_landmark">

  <input type="submit" value="Create New Figure">

</form>
